Place the OGG audio files here with the exact filenames:
- parapharmacie.ogg
- dispositifs_medicaux.ogg
- nutrition.ogg
- complements_alimentaires.ogg

You can replace these placeholder files with the real OGG binaries. If you want me to upload them to WhatsApp Cloud, confirm that `.env` contains valid `WHATSAPP_TOKEN` and `WHATSAPP_PHONE_NUMBER_ID` and I will perform the upload to obtain WhatsApp media IDs and update `database.py`.
